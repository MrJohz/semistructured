{
  "name": "semistructured",
  "version": "0.0.0",
  "description": "",
  "files": [
    "dist",
    "src"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "check": "tsc",
    "lint": "oxlint --max-warnings 0",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "test": "node --test --test-timeout 10000 src/**/*.test.ts",
    "test:wpt": "node --test --test-timeout 10000 test/wpt/wpt.test.ts",
    "ci": "npm run check && npm run lint && npm run fmt:check && npm test && npm run test:wpt",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "oxfmt": "latest",
    "oxlint": "latest",
    "typescript": "latest"
  }
}
