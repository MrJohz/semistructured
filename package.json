{
  "name": "semistructured",
  "version": "0.0.0",
  "description": "",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "check": "tsc",
    "lint": "oxlint --max-warnings 0",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "test": "NODE_OPTIONS='--expose-gc' node --test --test-timeout 10000 src/**/*.test.ts",
    "test:wpt": "node --test --test-timeout 10000 test/wpt/wpt.test.ts",
    "ci": "npm run check && npm run lint && npm run fmt:check && npm test && npm run test:wpt",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "oxfmt": "latest",
    "oxlint": "latest",
    "tsdown": "^0.21.7",
    "typescript": "latest"
  }
}
